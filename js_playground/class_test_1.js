function Player(name, attackDamage, healthPoints) {

  var playerCount = 0;

  function createPlayer() {
    if (l)
    playerCount++;
    playerCount
  }

  if (arguments.length == 0) {
    return playerCount;
  } else {
    return createPlayer;
  }


}

var name = 'Jacob';
var attack = 25;
var hp = 100;

var createPlayer = Player(name, attack, hp);

player = createPlayer();
player1 =
console.log(player);

// var player1 = Player(name, attack, hp);
// var player2 = Player(name, attack, hp);
